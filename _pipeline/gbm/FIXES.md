# FIXES — GBM 專題中文正文修訂指令（編輯裁決後）

修訂 agent 依組別修自己組的檔案。**`review-safety.md` 與 `review-voice.md` 裡屬於你這組檔案的條目
全部要處理**，除了下面「編輯否決」的。報告每條都附原句與替換句；替換句是建議，你可以寫得更好，
但問題必須消失。

## 改完必須維持的不變式
- 只用允許的標籤；內文 `[n]` 依首次出現順序連號；參考資料條目數與 href 逐條對應；
  全篇一個 `<hr>` 一個 `<h3>`；檔尾 `<p></p>`。
- 若某項修改刪掉了某個引用的最後一次出現，把該條參考資料一併刪除並全部重新編號（內文與清單都要）。
- 若需要新增引用，**只能從你這組 brief 的 PASS 來源拿**，書目照 brief 抄。
- **不要動任何一條既有參考資料的書目資料**（作者、年份、期刊、卷期頁、DOI）——201 筆已用程式對過
  Europe PMC，`verify_refs.py` 報的 5 筆「不符」與 20 筆「沒寫卷期頁」**全部是工具已知誤報**
  （`<sup>` 標記、頁碼縮寫 e56-93、討論頁併入 pageInfo、`Supplement_6` 底線、期別為 None 的文章編號式）。
  **不准「順手修正」書目資料。**
- h4 數維持 5–8；改了 h4 標題要回報（繪圖 manifest 的 placement 對 h4 全文）。
- 利益揭露段（A3、B1、B4、B5）一個字都不能動。
- 改完重跑 `python3 /home/claude/gbm/check_article_html.py body/<檔>.html --leads <臨時 leads.json>`，0 錯誤。

## 一、甲級十條全部照改（安全）
特別是這三條，不得打折：
1. `gb-elderly` 的 HR 2.01 方向會被讀反——同段補方向說明，且不得讓「那乾脆不要治了」成立。
2. `gb-warning-signs` 的 VTE 段落**改回「認得出症狀、當天回來」**：ITAC 的 grade 標示與
   「1B 不建議常規預防」不寫給病人（那是醫師的決定，修正 12）；OR 3.75 補「勝算比不是機率倍數」；
   PRODIGE 出血未達顯著（HR 4.2，P=0.22）要並陳，不可只留最嚇人的那句。
3. `gb-standard` 的 mg/m² 條列**加擋句**（「這不是給你的用法用量，劑量由你的團隊算」）；
   `gb-mgmt` 補一句「不要自己停藥」。

## 二、乙級十一條全部照改
含：兩年存活 27.2% 與 26.5% 同篇並存要統一；NRG-BN001 質子臂的 CI 與 p 值兩篇寫法要一致；
「給付條文對年齡沒有任何限制」「lomustine 給付規定欄是空的」兩處**刪掉從條文缺漏做的推論**；
駕照換照補「屆滿前後一個月內」；第 64-1 條沒用到就從參考資料刪除並重新編號；
`gb-mgmt`／`gb-numbers` 補 C4 指路；`gb-cognition` 指過去的症狀要對得上 C4 清單。

## 三、丙級：術語與數字統一（照 review-voice 的統一表）
- **整體存活**（`gb-recurrence` 等的「總存活」改掉）；**grade 4** 統一（不用「第 4 級」）；
  **風險比／勝算比／95% 信賴區間 a 到 b**（中文，不用 HR／CI 縮寫）；範圍一律「a 到 b」；
  年數用中文（兩年、三年、五年），Gy／人數／百分比用阿拉伯；四位數以上加千分位；
  P 值統一寫法；`gb-elderly` 一篇內中文與阿拉伯年齡並存要統一。
- **風險比第一次出現必須翻白話**（十七篇裡有七篇一次都沒翻，`gb-elderly` 用 12 次、
  `gb-newthings` 用 13 次）；**信賴區間也要翻一次**（十七篇全無）。
- **「無法治癒」改寫**：紅線 1 禁「治癒」二字，改成「目前沒有辦法把它清乾淨」這類講法。
- **§六 歸屬下 11 組重複展開**照 review-voice 逐條讓路：該讓的那一篇刪掉展開，改一句指路。

## 四、丁級：文風（作者這次要「像我平常講話」，照改）
- **粗體密度**：D 組 13.0/千字、C 組 7.3，壓到 A/B 的水準（3–5/千字）。
  `gb-recurrence` 的 14.88/千字是全系列最高，優先處理。
- **`<strong>標籤：</strong>` 開頭的 `<li>`**：`gb-warning-signs` 3/3、`gb-numbers` 3/3 要打散
  （WRITER-COMMON 明文禁止全部都用）。
- **「不是 X，是 Y」**：全系列 27%（基準文 10%），壓到 15% 以下；`gb-surgery` 4/6 優先。
- **收尾引導句**：12 篇用一字不改的「下次門診…問出口」，最多留 3 篇，其餘改成各自不同的收束。
- **「作者的結論句是」**：13 次，基準文 0 次，全部刪掉或改寫。
- **`gb-warning-signs` 的三級清單**要改寫成門診口吻（目前第一人稱 0.67/千字、零筆問號，
  是衛教單張的聲音）——但**警訊條目與行動門檻一條都不准刪**。
- **`note`（醫師的話）**：17 則都是同一個三拍模子，且 11 則砍掉了「先給立場再給動作」的第一拍；
  `gb-what-it-is` 與 `gb-pathology` 兩則是孿生，要拆開。各組自己重寫 note，形狀互異。
- `gb-seizure` 的口頭禪（「X 件事／X 層／是兩件事」10 處）壓到 3 次以下。
- `gb-steroid` 破折號零筆、C 組整體 0.49/千字——不是硬要加，但那一篇讀起來過於工整，
  自然一點。

## 五、字數放寬（編輯裁決）
- **`gb-newthings` 放寬到 2,800 字**：補回 Cochrane NMA 對 TTFields 的中性評語（平衡點）與
  TTFields 費用的「月費型支出」形態（照 §一2 的可及性寫法，不點名醫院、不寫金額）。
- **`gb-warning-signs` 放寬到 2,800 字**：補回被壓縮刪掉的量化細節（血小板低下的比例與天數、
  VTE 事件組成），警訊條目一條不減。
- 其餘各篇維持 1,600–2,400。超出上限的比例要在回報中記錄。

## 六、編輯否決（不要改）
- `gb-standard` 不寫 IMRT／VMAT 的處理**維持**——沒有可引來源支撐那兩個名詞，寫成
  「照哪一台機器這一段我不寫」是對的。
- `gb-newthings` 與 `gb-recurrence` 對 GBM AGILE 的細節密度不對稱**維持**（B 組有 HR、D 組定性），
  兩者不矛盾。
- `verify_refs.py` 報的所有書目「不符」**一律不改**（見不變式）。

## 回報格式
每篇：改了哪些條（甲/乙/丙/丁編號）、字數／引用數／h4 數、h4 標題是否變動（列新舊）、
note 是否重寫、meta 是否變動。
